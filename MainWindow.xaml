<Window x:Class="SystemOptimizerLite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="系统优化工具 Lite"
        Height="760"
        Width="1120"
        MinWidth="920"
        MinHeight="620"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource Bg}"
        Foreground="{DynamicResource Text}"
        WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            NonClientFrameEdges="None"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2B2B2B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#343434"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseTitleBarButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#7A1F2A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#9B2532"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MoreMenuItemStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>
    </Window.Resources>

    <Grid ClipToBounds="True" UseLayoutRounding="True">
        <Border x:Name="WindowFrameBorder" BorderThickness="0" CornerRadius="0" Background="{DynamicResource Bg}" SnapsToDevicePixels="True" ClipToBounds="True">
            <Grid x:Name="WindowRootGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="218"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="SidebarBorder" Grid.Column="0" Background="{DynamicResource Sidebar}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,1,0" CornerRadius="0">
                <DockPanel Margin="16">
                    <StackPanel DockPanel.Dock="Top" Margin="4,18,0,24">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="36" Height="36" CornerRadius="11" Background="{DynamicResource Blue}">
                                <TextBlock Text="W" Foreground="White" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Windows Lite" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="System Optimizer v3.1" Foreground="{DynamicResource Muted}" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <Grid DockPanel.Dock="Bottom" Margin="0,12,0,4">
                        <Button x:Name="MoreButton" Content="•••" Style="{StaticResource GhostButtonStyle}" Width="42" Height="34" HorizontalAlignment="Left" Click="MoreButton_Click" ToolTip="更多"/>
                        <Popup x:Name="MorePopup" PlacementTarget="{Binding ElementName=MoreButton}" Placement="Top" StaysOpen="False" AllowsTransparency="True" PopupAnimation="Fade" HorizontalOffset="0" VerticalOffset="-8">
                            <Border Background="{DynamicResource Panel2}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="14" Padding="7" Width="190" SnapsToDevicePixels="True">
                                <StackPanel>
                                    <Button Content="导出日志" Style="{StaticResource MoreMenuItemStyle}" Click="ExportLogs_Click"/>
                                    <Button Content="关于软件" Style="{StaticResource MoreMenuItemStyle}" Click="About_Click"/>
                                    <Button Content="帮助文档" Style="{StaticResource MoreMenuItemStyle}" Click="Help_Click"/>
                                    <Button Content="反馈问题" Style="{StaticResource MoreMenuItemStyle}" Margin="0" IsEnabled="False"/>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>

                    <StackPanel x:Name="NavPanel" HorizontalAlignment="Stretch"/>
                </DockPanel>
            </Border>

            <Grid x:Name="MainContentGrid" Grid.Column="1" ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="42"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource Bg}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" SnapsToDevicePixels="True">
                    <DockPanel LastChildFill="False" Margin="20,0,0,0">
                        <StackPanel x:Name="TopStatusPanel" Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Top" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                            <Button Content="−" Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click"/>
                            <Button Content="□" Style="{StaticResource TitleBarButtonStyle}" Click="Maximize_Click"/>
                            <Button Content="×" Style="{StaticResource CloseTitleBarButtonStyle}" Click="Close_Click"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Grid Grid.Row="1" Margin="32,22,32,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="260"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,14,0">
                        <TextBlock x:Name="TitleText" Text="系统优化" FontSize="30" FontWeight="SemiBold"/>
                        <TextBlock x:Name="SubtitleText" Text="轻量、安全、可回退的系统优化工具" Foreground="{DynamicResource Muted}" FontSize="14" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <WrapPanel x:Name="PageActionPanel" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" MaxWidth="338"/>
                </Grid>

                <ScrollViewer x:Name="ContentScrollViewer" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="32,0,26,0">
                    <StackPanel x:Name="ContentPanel"/>
                </ScrollViewer>

                <TextBlock x:Name="StatusText" Grid.Row="3" Text="就绪" Foreground="{DynamicResource Muted}" FontSize="12" Margin="34,8,30,16"/>

            </Grid>

            <Border x:Name="HoverInfoPopup"
                    Grid.ColumnSpan="2"
                    Panel.ZIndex="100"
                    Visibility="Collapsed"
                    Opacity="0"
                    IsHitTestVisible="False"
                    MaxWidth="440"
                    Background="{DynamicResource Panel2}"
                    BorderBrush="{DynamicResource Border}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="10,7,10,8"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="HoverInfoTransform"/>
                </Border.RenderTransform>
                <TextBlock x:Name="HoverInfoText"
                           Foreground="{DynamicResource Text}"
                           TextWrapping="Wrap"
                           MaxWidth="420"
                           LineHeight="19"
                           FontSize="13"/>
            </Border>

            <Grid x:Name="DrawerOverlay" Grid.ColumnSpan="2" Panel.ZIndex="200" Visibility="Collapsed" Opacity="0" Background="#66000000">
                <Border Background="Transparent" MouseLeftButtonDown="DrawerBackdrop_MouseLeftButtonDown"/>
                <Border x:Name="DrawerPanel" Width="460" HorizontalAlignment="Right" Margin="12" Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="18" KeyboardNavigation.TabNavigation="Cycle">
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="DrawerTransform" X="460"/>
                    </Border.RenderTransform>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="24,22,18,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock x:Name="DrawerTitle" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <TextBlock x:Name="DrawerSubtitle" Foreground="{DynamicResource Muted}" FontSize="13" TextWrapping="Wrap" Margin="0,6,16,0" LineHeight="20"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="×" Style="{StaticResource GhostButtonStyle}" Width="38" Height="38" FontSize="18" Click="DrawerClose_Click"/>
                        </Grid>
                        <ScrollViewer x:Name="DrawerScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="24,0,18,0">
                            <StackPanel x:Name="DrawerContent"/>
                        </ScrollViewer>
                        <Border Grid.Row="2" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0" Padding="20,16">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="DrawerCancelButton" Content="关闭" MinWidth="88" Margin="0,0,8,0" Click="DrawerClose_Click"/>
                                <Button x:Name="DrawerSecondaryButton" Content="次要操作" MinWidth="112" Margin="0,0,8,0" Click="DrawerSecondary_Click" Visibility="Collapsed"/>
                                <Button x:Name="DrawerPrimaryButton" Content="确认" Style="{StaticResource PrimaryButtonStyle}" MinWidth="112" Click="DrawerPrimary_Click"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
            </Grid>
        </Border>
        <Border x:Name="WindowOutlineBorder" BorderBrush="{DynamicResource WindowBorder}" BorderThickness="1" CornerRadius="16" IsHitTestVisible="False" SnapsToDevicePixels="True"/>
    </Grid>
</Window>
