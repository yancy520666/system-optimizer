<Window x:Class="SystemOptimizerLite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="系统优化工具 Lite"
        Height="700"
        Width="960"
        MinWidth="920"
        MinHeight="620"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource Bg}"
        Foreground="{DynamicResource Text}"
        WindowStartupLocation="CenterScreen">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="8"
                            GlassFrameThickness="0"
                            NonClientFrameEdges="None"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2B2B2B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#343434"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseTitleBarButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#7A1F2A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#9B2532"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderThickness="1" BorderBrush="{DynamicResource Border}" CornerRadius="8" Background="{DynamicResource Bg}" SnapsToDevicePixels="True">
        <Grid x:Name="WindowRootGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="198"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource Sidebar}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,1,0">
                <DockPanel Margin="14">
                    <StackPanel DockPanel.Dock="Top" Margin="0,18,0,22" HorizontalAlignment="Center">
                        <TextBlock Text="系统优化工具" FontSize="18" FontWeight="SemiBold" TextAlignment="Center"/>
                        <TextBlock Text="v 3.0 Lite" Foreground="{DynamicResource Muted}" FontSize="12" Margin="0,4,0,0" TextAlignment="Center"/>
                    </StackPanel>

                    <Grid DockPanel.Dock="Bottom" Margin="0,12,0,4">
                        <Button x:Name="MoreButton" Content="☰" Width="42" Height="34" HorizontalAlignment="Left" Click="MoreButton_Click" ToolTip="更多"/>
                        <Popup x:Name="MorePopup" PlacementTarget="{Binding ElementName=MoreButton}" Placement="Top" StaysOpen="False">
                            <Border Background="{DynamicResource Panel2}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8" Padding="6" Width="170">
                                <StackPanel>
                                    <Button Content="导出日志" HorizontalContentAlignment="Left" Margin="0,0,0,5" Click="ExportLogs_Click"/>
                                    <Button Content="关于软件" HorizontalContentAlignment="Left" Margin="0,0,0,5" Click="About_Click"/>
                                    <Button Content="帮助文档" HorizontalContentAlignment="Left" Margin="0,0,0,5" Click="Help_Click"/>
                                    <Button Content="反馈问题" HorizontalContentAlignment="Left" IsEnabled="False"/>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>

                    <StackPanel x:Name="NavPanel"/>
                </DockPanel>
            </Border>

            <Grid x:Name="MainContentGrid" Grid.Column="1" ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="42"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource Bg}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" SnapsToDevicePixels="True">
                    <DockPanel LastChildFill="False" Margin="20,0,0,0">
                        <StackPanel x:Name="TopStatusPanel" Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Top" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                            <Button Content="−" Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click"/>
                            <Button Content="□" Style="{StaticResource TitleBarButtonStyle}" Click="Maximize_Click"/>
                            <Button Content="×" Style="{StaticResource CloseTitleBarButtonStyle}" Click="Close_Click"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Grid Grid.Row="1" Margin="30,18,30,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="260"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,14,0">
                        <TextBlock x:Name="TitleText" Text="系统优化" FontSize="28" FontWeight="Bold"/>
                        <TextBlock x:Name="SubtitleText" Text="轻量、安全、可回退的系统优化工具" Foreground="{DynamicResource Muted}" FontSize="14" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <WrapPanel x:Name="PageActionPanel" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" MaxWidth="338"/>
                </Grid>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="30,0,30,0">
                    <StackPanel x:Name="ContentPanel"/>
                </ScrollViewer>

                <TextBlock x:Name="StatusText" Grid.Row="3" Text="就绪" Foreground="{DynamicResource Muted}" Margin="32,8,30,14"/>

            </Grid>

            <Border x:Name="HoverInfoPopup"
                    Grid.ColumnSpan="2"
                    Panel.ZIndex="100"
                    Visibility="Collapsed"
                    Opacity="0"
                    IsHitTestVisible="False"
                    Width="460"
                    MaxWidth="520"
                    Background="{DynamicResource Panel2}"
                    BorderBrush="{DynamicResource Border}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="10,7,10,8"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="HoverInfoTransform"/>
                </Border.RenderTransform>
                <TextBlock x:Name="HoverInfoText"
                           Foreground="{DynamicResource Text}"
                           TextWrapping="Wrap"
                           LineHeight="19"
                           FontSize="13"/>
            </Border>
        </Grid>
    </Border>
</Window>
